<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Github博客制作</title>
    <url>/blogArchitectureAnalysis/</url>
    <content><![CDATA[<h2 id="前情提要-无用"><a href="#前情提要-无用" class="headerlink" title="前情提要 (无用)"></a>前情提要 (<del>无用</del>)</h2><hr>
<p>出于方便复习、找工作、<del>装呗</del>等各种原因，博主开始了编写github博客，构建历程还是很坎坷的，期间由于信息闭塞，一开始选择手动构建博客框架，吃了不少苦头，最后选择了使用hexo来构建博客，与原来相同的工作量可以构建出更精美漂亮的博客，还兼容多种插件如gitalk等等。</p>
<p>如果对手动构建博客还感兴趣的朋友可以参考本仓库的legacy-main分支，也就是我的构建博客废案</p>
<p>简单介绍一下：</p>
<pre><code>posts文件夹 存放想要发布的文章

images文件夹 存放图片

index.md 是github page自动生成博客首页的md源文件
</code></pre>
<h2 id="博客架构构建"><a href="#博客架构构建" class="headerlink" title="博客架构构建"></a>博客架构构建</h2><hr>
<p>通过右上角可以直接看到我的博客源代码，你会发现一大串的文件夹与文件，不用害怕，绝大多数都是hexo自动生成的（赞美前人）。</p>
<p>你需要准备的有：github账号、本地git（可选）、node.js(只需安装)、以及markdown和yml的基础语法知识等等</p>
<p>这里有一篇我找到的最无脑向的教程</p>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/371995929">前往知乎看教程</a></li>
</ul>
<p><strong>注意</strong> ：该文章关于gitalk等额外甜品功能的配置，说明得很潦草，可以参考下面这两篇博客</p>
<ul>
<li><a href="https://kuroha.vip/hexo/next_theme_beautify.html#%E6%B7%BB%E5%8A%A0%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F">前往看博客</a></li>
<li><a href="https://yuanlichenai.cn/2020/01/16/Gitalk/">gitalk专场</a></li>
</ul>
<p>如果你看完了上面的教程，那你肯定已经构建好一个基本的博客框架了。如果你想要和我一样有一个主页实现目录功能，需要参考接下来的部分</p>
<h2 id="目录文章构建"><a href="#目录文章构建" class="headerlink" title="目录文章构建"></a>目录文章构建</h2><hr>
<p><del>单纯从不想浪费之前的手工制作主页</del></p>
<ul>
<li><a href="../index/index.html">提前看一眼实现效果</a></li>
</ul>
<p>由于使用了hexo框架，主页默认展示的是文章的集合，这部分我不打算更改（<del>菜的</del>），因为指向自定义的主页会与已有框架主题不兼容，而且框架本身提供了categories功能，可以实现分类功能</p>
<p> 可以看到我的主页包含了一个可折叠的分类菜单，这部分据查在markdown中当下挂项目过多时由渲染自动实现，但我是通过html手工实现了一个。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">details</span>&gt;</span></span><span class="language-xml"><span class="tag">&lt;<span class="name">summary</span> <span class="attr">markdown</span>=<span class="string">&quot;span&quot;</span>&gt;</span></span>Github博客制作<span class="language-xml"><span class="tag">&lt;/<span class="name">summary</span>&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> [<span class="string">博客架构解析</span>](<span class="link">../hello-world/index.html</span>) </span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> [<span class="string">语法注意事项</span>](<span class="link">https://example.com</span>)</span><br><span class="line"><span class="language-xml"><span class="tag">&lt;/<span class="name">details</span>&gt;</span></span></span><br></pre></td></tr></table></figure>

<p>这是md文件中的写法，但是，坑人的来了——github page默认的md解释器是不支持在html代码块中解释md代码的，所以，想要实现折叠功能，需要在博客项目根目录下的_config.yml文件中加上如下代码:</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># The release of Jekyll Now</span></span><br><span class="line"><span class="attr">version:</span> <span class="string">v1.2.0</span></span><br><span class="line"></span><br><span class="line"><span class="attr">markdown:</span> <span class="string">kramdown</span></span><br><span class="line"><span class="attr">kramdown:</span></span><br><span class="line"><span class="comment"># Use GitHub flavored markdown, including triple backtick fenced code blocks</span></span><br><span class="line">  <span class="attr">input:</span> <span class="string">GFM</span></span><br><span class="line">  <span class="attr">parse_block_html:</span> <span class="literal">true</span>  <span class="comment"># 启用HTML块内Markdown解析</span></span><br></pre></td></tr></table></figure>
<p>综上，你就可以通过你的md知识，构建出一个简单的目录文章（<del>我可没有在骂人</del>）</p>
<h2 id="语法注意事项"><a href="#语法注意事项" class="headerlink" title="语法注意事项"></a>语法注意事项</h2><hr>
<p>如何在文章中引用本站文章呢(不通过htttp形式的链接)?</p>
<p>由于你的文章md文件保存在theme&#x2F;%yourTheme&#x2F;source&#x2F;posts目录下，</p>
<p>假设你当前在文章1，md传统写法是</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[跳转！](文章2.md)</span><br></pre></td></tr></table></figure>
<p>Buuuuuut！！！因为hexo框架的设计，实际跳转是在public中找到生成好的html文件，而上述代码被框架翻译来就是，在public&#x2F;文章1&#x2F;文件夹下寻找名为文章2.md的文件</p>
<p>此时public文件夹内容如下</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">public:</span></span><br><span class="line">  <span class="string">文章1：</span></span><br><span class="line">    <span class="string">index.html</span></span><br><span class="line">  <span class="string">文章2：</span></span><br><span class="line">    <span class="string">index.html</span></span><br></pre></td></tr></table></figure>
<p>所以，正确的写法是</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[跳转！](../文章1/index.html)</span><br></pre></td></tr></table></figure>
<p>注意public下html存放的文件夹的名称与项目根目录下_config.yml文件中的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">permalink: :title/ #这条参数有关，我设置的是title，避免了日期格式的多重目录</span><br></pre></td></tr></table></figure>







]]></content>
      <categories>
        <category>-blog</category>
      </categories>
      <tags>
        <tag>-blog</tag>
      </tags>
  </entry>
  <entry>
    <title>OutFRid 的个人博客</title>
    <url>/index/</url>
    <content><![CDATA[<h2 id="欢迎来到我的个人博客"><a href="#欢迎来到我的个人博客" class="headerlink" title="欢迎来到我的个人博客"></a>欢迎来到我的个人博客</h2><hr>
<p><img src="/images/index.jpg" alt="主题照片"></p>
<hr>
<h2 id="我的文章"><a href="#我的文章" class="headerlink" title="我的文章"></a>我的文章</h2><ul>
<li><a href="../blogArchitectureAnalysis/index.html">Github博客制作</a></li>
</ul>
<details><summary markdown="span">Python基础</summary>

<ul>
<li><p><a href="../pyAio/index.html">python代码源码、字节码分析方法</a></p>
</li>
<li><p><a href="../pyDis/index.html">Python多进程、多线程、多协程浅解</a></p>
</li>
<li><p>[Python上下文管理器浅解](Context manager)</p>
</details></li>
</ul>
<p><strong><del>暂时还没有其他更新</del></strong></p>
]]></content>
      <tags>
        <tag>-index</tag>
      </tags>
  </entry>
  <entry>
    <title>python代码源码、字节码分析方法</title>
    <url>/pyDis/</url>
    <content><![CDATA[<h3 id="简单介绍"><a href="#简单介绍" class="headerlink" title="简单介绍"></a>简单介绍</h3><p>这是一个python包，需要通过import使用，来看看官方的说明</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">dis 模块通过反汇编支持CPython的 bytecode 分析。该模块作为输入的 CPython 字节码在文件 Include/opcode.h 中定义，并由编译器和解释器使用。</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="CPython"><a href="#CPython" class="headerlink" title="CPython"></a>CPython</h3><p>很好，引出了个新概念 CPython , 这是什么东西？</p>
<p>CPython即用C语言环境实现Python及其解释器， 根据Python实现方法的不同，有Cpython, Jython, Pypy等</p>
<p>比如Jython就是跑在JVM上的，我的理解上，就是通过不同的底层环境实现的不同Python解释器，就如同用机器汇编语言实现C语言一样，本质上就是编译器的实现环境、实现细节以及支持功能等等不同</p>
<p>就好像博主曾经用Python写过一个功能不全的C语言编译器，在这里可能就会被叫做PyC。</p>
<p>而CPython是当前应用最广泛即默认的Python解释器</p>
<hr>
<h3 id="功能描述"><a href="#功能描述" class="headerlink" title="功能描述"></a>功能描述</h3><p>那 dis 这个模块有什么作用呢？</p>
<p>如果你只是想用Python写项目、跑AI、做开发，这个模块几乎用不到，但是这个模块可以将Python代码反汇编为字节码，更好的查看你的Python代码到底做了什么事情，从而在代码实现的角度进行时间、空间上的优化，或是发现原来代码中可能出现的BUG，这种BUG不是你的Python语法、设计思路有问题，而有可能是解释器本身不支持这样写。</p>
<hr>
<h3 id="示例体验"><a href="#示例体验" class="headerlink" title="示例体验"></a>示例体验</h3><p>给定函数 myfunc（）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">myfunc</span>(<span class="params">alist</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">len</span>(alist)</span><br></pre></td></tr></table></figure>
<p><img src="/images/disFunc1.png" alt="IDE图片"><br>调用命令</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">dis.dis(myfunc)</span><br></pre></td></tr></table></figure>
<p>输出</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"> <span class="number">57</span>           <span class="number">0</span> RESUME                   <span class="number">0</span></span><br><span class="line"></span><br><span class="line"> <span class="number">58</span>           <span class="number">2</span> LOAD_GLOBAL              <span class="number">1</span> (NULL + <span class="built_in">len</span>)</span><br><span class="line">             <span class="number">14</span> LOAD_FAST                <span class="number">0</span> (alist)</span><br><span class="line">             <span class="number">16</span> PRECALL                  <span class="number">1</span></span><br><span class="line">             <span class="number">20</span> CALL                     <span class="number">1</span></span><br><span class="line">             <span class="number">30</span> RETURN_VALUE</span><br><span class="line"><span class="comment">#其中 57、58是IDE中的行号</span></span><br></pre></td></tr></table></figure>
<p>另举一例做各部分解释：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">f</span>(<span class="params">num</span>):</span><br><span class="line">    <span class="keyword">if</span> num == <span class="number">42</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">​</span><br><span class="line">​</span><br><span class="line"><span class="keyword">import</span> dis  <span class="comment"># 导入dis库</span></span><br><span class="line">dis.dis(f) </span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">(<span class="number">1</span>)|(<span class="number">2</span>)|(<span class="number">3</span>)|(<span class="number">4</span>)|          (<span class="number">5</span>)         |(<span class="number">6</span>)|  (<span class="number">7</span>)</span><br><span class="line">---|---|---|---|----------------------|---|-------</span><br><span class="line">  <span class="number">2</span>|   |   |  <span class="number">0</span>|LOAD_FAST             |  <span class="number">0</span>|(num)</span><br><span class="line">   |--&gt;|   |  <span class="number">2</span>|LOAD_CONST            |  <span class="number">1</span>|(<span class="number">42</span>)</span><br><span class="line">   |   |   |  <span class="number">4</span>|COMPARE_OP            |  <span class="number">2</span>|(==)</span><br><span class="line">   |   |   |  <span class="number">6</span>|POP_JUMP_IF_FALSE     | <span class="number">12</span>|</span><br><span class="line">   |   |   |   |                      |   |</span><br><span class="line">  <span class="number">3</span>|   |   |  <span class="number">8</span>|LOAD_CONST            |  <span class="number">2</span>|(<span class="literal">True</span>)</span><br><span class="line">   |   |   | <span class="number">10</span>|RETURN_VALUE          |   |</span><br><span class="line">   |   |   |   |                      |   |</span><br><span class="line">  <span class="number">4</span>|   |&gt;&gt; | <span class="number">12</span>|LOAD_CONST            |  <span class="number">3</span>|(<span class="literal">False</span>)</span><br><span class="line">   |   |   | <span class="number">14</span>|RETURN_VALUE          |   |</span><br></pre></td></tr></table></figure>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">第1列: 源代码中的相应行号. </span><br><span class="line">第2列: 可选地指示执行的当前指令(例如，当字节码来自框架对象时). </span><br><span class="line">第3列: 一个标签，表示从早期指令到此指令的可能JUMP. </span><br><span class="line">第4列: 字节码中与字节索引相对应的地址(这些是2的倍数，因为Python 3.6对每条指令使用2字节，而之前的版本可能会有所不同). </span><br><span class="line">第5列: 指令名称(也称为opname)，每个指令名称都在dis模块中简要解释，它们的实现可以在ceval.c(CPython的核心文件)中找到. </span><br><span class="line">第6列: Python内部用于获取一些常量或变量、管理堆栈、跳转到特定指令等的指令的参数(如果有的话). </span><br><span class="line">第7列: 对解释器的参数的人性化的解释. </span><br></pre></td></tr></table></figure>

<hr>
<p>其余详细参数以及使用方法可以参照官方文档<br><a href="https://docs.python.org/zh-cn/3.13/library/dis.html">dis官方文档链接</a></p>
]]></content>
      <categories>
        <category>-pyTutorial</category>
      </categories>
      <tags>
        <tag>-python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python多进程、多线程、多协程浅解</title>
    <url>/pyAio/</url>
    <content><![CDATA[<h2 id="多进程"><a href="#多进程" class="headerlink" title="多进程"></a>多进程</h2><p>Python多进程是一种通过操作系统级资源分配实现并发执行的技术，其核心在于利用多个独立的进程并行处理任务。</p>
<h3 id="功能说明"><a href="#功能说明" class="headerlink" title="功能说明"></a>功能说明</h3><ul>
<li><p>并行计算</p>
<p>Python多进程通过 multiprocessing 模块实现真正的并行执行，每个进程拥有独立的内存空间和执行环境，可同时利用多核CPU。</p>
</li>
<li><p>资源分配与隔离</p>
<p>进程是操作系统分配资源的基本单位，每个进程独立运行，互不干扰。进程的动态性体现在其生命周期中，从创建到终止均由系统管理。多进程的隔离性使其在稳定性上优于多线程，尤其适合CPU密集型任务。</p>
</li>
<li><p>通信与同步机制</p>
<ul>
<li>进程间通信（IPC） ：通过Queue（队列）、Pipe（管道）、Manager（共享内存）等工具实现数据传递。Queue适用于主进程与子进程之间的数据交换，而Manager支持复杂数据结构的共享。</li>
<li>同步原语：使用Lock、Barrier等同步对象协调多个进程的执行顺序，避免竞争条件。</li>
</ul>
</li>
<li><p>进程池管理</p>
<p>multiprocessing.Pool 简化了批量任务的并行处理。通过 pool.map() 可以将任务分配给进程池，自动管理进程生命周期。</p>
</li>
</ul>
<h3 id="实现方法"><a href="#实现方法" class="headerlink" title="实现方法"></a>实现方法</h3><ul>
<li><p>直接创建进程</p>
<ul>
<li><p>Process类</p>
<p>通过multiprocessing.Process创建进程对象，指定target函数和args参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> multiprocessing</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">worker1</span>():</span><br><span class="line">    <span class="comment"># printing process id</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;ID of process running worker1: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(os.getpid()))</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">worker2</span>():</span><br><span class="line">    <span class="comment"># printing process id</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;ID of process running worker2: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(os.getpid()))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&quot;__main__&quot;</span>:</span><br><span class="line">    <span class="comment"># printing main program process id</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;ID of main process: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(os.getpid()))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># creating processes</span></span><br><span class="line">    p1 = multiprocessing.Process(target=worker1)</span><br><span class="line">    p2 = multiprocessing.Process(target=worker2)</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;haven\&#x27;t started yet&#x27;</span>)</span><br><span class="line">    <span class="comment"># starting processes</span></span><br><span class="line">    p1.start()</span><br><span class="line">    p2.start()</span><br><span class="line">    <span class="comment"># process IDs</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;ID of process p1: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(p1.pid))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;ID of process p2: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(p2.pid))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;started&#x27;</span>)</span><br><span class="line">    <span class="comment"># wait until processes are finished</span></span><br><span class="line">    p1.join()</span><br><span class="line">    p2.join()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># both processes finished</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Both processes finished execution!&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># check if processes are alive</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Process p1 is alive: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(p1.is_alive()))</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Process p2 is alive: &#123;&#125;&quot;</span>.<span class="built_in">format</span>(p2.is_alive()))</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;  输出结果  </span></span><br><span class="line"><span class="string">    ID of main process: 29952</span></span><br><span class="line"><span class="string">    haven&#x27;t started yet</span></span><br><span class="line"><span class="string">    ID of process p1: 9956</span></span><br><span class="line"><span class="string">    ID of process p2: 11804</span></span><br><span class="line"><span class="string">    started</span></span><br><span class="line"><span class="string">    ID of process running worker1: 9956</span></span><br><span class="line"><span class="string">    ID of process running worker2: 11804</span></span><br><span class="line"><span class="string">    Both processes finished execution!</span></span><br><span class="line"><span class="string">    Process p1 is alive: False</span></span><br><span class="line"><span class="string">    Process p2 is alive: False</span></span><br><span class="line"><span class="string">    &#x27;&#x27;&#x27;</span></span><br></pre></td></tr></table></figure>
<p>可以看到p1.start()之后并不直接执行，而是继续执行主进程，直到p1.join()，将主进程阻塞至p1执行完毕</p>
</li>
<li><p>守护进程</p>
<p>通过 daemon&#x3D;True 设置进程为守护进程，主进程退出时自动终止子进程</p>
<p>借助 Process类 中的代码可以看到不设置 daemon 主程序只管自己结束，然后子程序还在运行</p>
<p>设置了 daemon 主程序结束了 就全结束了  所以要设置 daemon+join 阻塞主程序，等待子程序运行结束后在运行主程序</p>
<p>即daemon一般与join配套使用</p>
</li>
</ul>
</li>
<li><p>继承Process类</p>
<p>自定义进程类继承multiprocessing.Process，重写run()方法。例如</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyProcess</span>(<span class="title class_ inherited__">Process</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, name</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="variable language_">self</span>.name = name</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&#x27;测试<span class="subst">&#123;self.name&#125;</span>多进程&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    p = MyProcess(<span class="string">&#x27;bob&#x27;</span>)</span><br><span class="line">    p.start()</span><br><span class="line"><span class="comment">#输出结果：</span></span><br><span class="line"><span class="comment">#测试bob多进程</span></span><br></pre></td></tr></table></figure>
<p>记住一定是 run 方法才会被视作 target 方法执行。</p>
</li>
</ul>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul>
<li><p>性能开销</p>
<ul>
<li>进程启动成本：每次创建进程需复制内存和资源，开销远高于线程。一般根据服务器配置调整进程数量，避免过度消耗。</li>
<li>通信开销：进程间通信（如Queue）需额外开销，需权衡效率与复杂度</li>
</ul>
</li>
<li><p>守护进程设置</p>
<ul>
<li>Windows系统要求进程代码位于if <strong>name</strong> &#x3D;&#x3D; ‘<strong>main</strong>‘块内，否则可能导致死循环（当然，好的IDE你不写在if <strong>name</strong> &#x3D;&#x3D; ‘<strong>main</strong>‘块内，会自动报错）</li>
<li>守护进程的 daemon 属性需谨慎设置，避免主进程退出时子进程残留</li>
</ul>
</li>
<li><p>同步与竞争条件</p>
<ul>
<li>共享内存风险：直接共享变量（如全局变量）可能导致数据不一致，需使用Lock或RLock同步。另外子进程与父进程不共享内存空间，即子进程获得父进程环境变量的独立副本，子进程对环境的修改不影响父进程，反之亦然</li>
<li>死锁问题：多进程间的同步逻辑需避免死锁，例如使用Barrier协调多个进程的等待状态</li>
</ul>
</li>
</ul>
<hr>
<h2 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h2><h3 id="功能覆盖"><a href="#功能覆盖" class="headerlink" title="功能覆盖"></a>功能覆盖</h3><ul>
<li><p>并发处理 </p>
<p>多线程可同时执行多个任务，例如文件读写、网络请求等I&#x2F;O密集型操作，避免主线程阻塞，提升程序响应性</p>
</li>
<li><p>任务分解与并行执行</p>
<p>将复杂任务拆分为子任务，通过多线程并行处理，缩短总耗时。例如，爬虫任务中可同时抓取多个网页(网卡也是IO设备)</p>
</li>
<li><p>资源共享与通信</p>
<p>线程间共享进程内存空间，便于数据传递和协作，但需注意同步问题</p>
</li>
<li><p>适用场景</p>
<ul>
<li>I&#x2F;O密集型任务：如文件读写、网络请求、数据库操作等，线程在等待I&#x2F;O时可切换执行其他任务。</li>
<li>用户界面优化：GUI应用中，后台任务（如进度计算）可与界面交互，避免卡顿。</li>
<li>异步任务处理：如定时任务、监控任务等，可通过多线程实现非阻塞执行</li>
</ul>
</li>
</ul>
<h3 id="实现方法-1"><a href="#实现方法-1" class="headerlink" title="实现方法"></a>实现方法</h3><ul>
<li><p>基础模块</p>
<p>Python通过threading模块实现多线程，其核心功能包括：</p>
<ul>
<li><p>Thread类 用于创建线程，支持两种方式：</p>
<ul>
<li>基于函数：直接指定任务函数</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">task</span>():</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;线程执行中&quot;</span>)</span><br><span class="line">t = threading.Thread(target=task)</span><br><span class="line">t.start()  <span class="comment"># [[2]]</span></span><br></pre></td></tr></table></figure>
<ul>
<li>基于类继承 自定义类继承Thread，重写run()方法</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyThread</span>(threading.Thread):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;自定义线程执行&quot;</span>)</span><br><span class="line">my_thread = MyThread()</span><br><span class="line">my_thread.start()  <span class="comment"># [[16]]</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>高级功能</p>
<ul>
<li>锁机制：使用 Lock 、RLock 等同步原语保护共享资源，避免数据竞争</li>
<li>守护线程：通过 setDaemon(True) 设置子线程随主线程结束而终止</li>
<li>线程池：通过 concurrent.futures.ThreadPoolExecutor 管理线程池，简化并发任务调度</li>
</ul>
</li>
</ul>
<h3 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h3><ul>
<li><p>全局解释器锁（GIL）</p>
<p>Python的GIL确保同一时间仅有一个线程执行字节码，导致CPU密集型任务无法真正并行</p>
<ul>
<li><p>解决方案</p>
<ul>
<li>I&#x2F;O密集型任务：利用多线程并发处理，提升效率</li>
<li>CPU密集型任务：改用多进程（multiprocessing）</li>
</ul>
</li>
</ul>
</li>
<li><p>线程安全与同步</p>
<ul>
<li>数据竞争：多个线程访问共享变量时可能导致不可预测的结果，需使用锁或信号量同步</li>
<li>死锁：避免锁的嵌套或循环等待，确保锁获取顺序一致</li>
<li>异常处理：线程异常不会自动终止程序，需通过try-except捕获异常</li>
</ul>
</li>
<li><p>性能优化</p>
<ul>
<li>线程数量：过多线程会增加上下文切换开销，建议根据CPU核心数合理分配</li>
<li>守护线程：设置daemon&#x3D;True可避免孤儿线程占用资源。</li>
<li>资源管理：及时释放锁（如lock.release()）防止死锁</li>
</ul>
</li>
</ul>
<hr>
<h2 id="多协程"><a href="#多协程" class="headerlink" title="多协程"></a>多协程</h2><p>开始之前给大佬视频引流 -<a href="https://www.bilibili.com/video/BV1oa411b7c9/?spm_id_from=333.337.search-card.all.click&vd_source=f6ad0f3e9d9d63873712e8c07454b756">bilibili高天讲解</a></p>
<h3 id="Python多协程的核心概念"><a href="#Python多协程的核心概念" class="headerlink" title="Python多协程的核心概念"></a>Python多协程的核心概念</h3><ul>
<li><p>定义与原理</p>
<p>多协程（Coroutine）是一种用户态的轻量级线程，通过协作式多任务实现并发。与操作系统线程不同：</p>
<ul>
<li><p>切换开销极低：协程上下文切换发生在用户空间，无需内核介入</p>
</li>
<li><p>非抢占式调度：由事件循环（Event Loop）管理，协程主动让出执行权（await）时才会切换。</p>
</li>
<li><p>适用于IO密集型任务：如网络请求、文件读写等阻塞操作，在等待期间自动挂起并执行其他协程</p>
</li>
</ul>
</li>
<li><p>与线程&#x2F;进程的对比</p>
<table>
<thead>
<tr>
<th align="center">维度</th>
<th align="center">协程</th>
<th align="center">线程</th>
<th align="center">进程</th>
</tr>
</thead>
<tbody><tr>
<td align="center">切换开销</td>
<td align="center">微秒级（用户态切换）</td>
<td align="center">毫秒级（内核态切换）</td>
<td align="center">最高（资源独立）</td>
</tr>
<tr>
<td align="center">内存占用</td>
<td align="center">KB级（动态扩缩容）</td>
<td align="center">MB级（固定栈大小）</td>
<td align="center">GB级（独立内存空间）</td>
</tr>
<tr>
<td align="center">并发能力</td>
<td align="center">单进程可支持数万协程</td>
<td align="center">受限于OS线程数（通常数百）</td>
<td align="center">数十个</td>
</tr>
<tr>
<td align="center">适用场景</td>
<td align="center">IO密集型（网络&#x2F;文件）</td>
<td align="center">IO&#x2F;CPU混合型</td>
<td align="center">CPU密集型</td>
</tr>
<tr>
<td align="center">GIL影响</td>
<td align="center">不受限（单线程事件循环）</td>
<td align="center">受CPython的GIL限制</td>
<td align="center">无影响</td>
</tr>
</tbody></table>
</li>
</ul>
<h3 id="多协程的具体实现与使用"><a href="#多协程的具体实现与使用" class="headerlink" title="多协程的具体实现与使用"></a>多协程的具体实现与使用</h3><ul>
<li><p>核心库：asyncio</p>
<p>Python通过asyncio标准库提供协程支持，核心组件包括</p>
<ul>
<li>事件循环（Event Loop） ：调度协程的核心引擎。</li>
<li>协程函数（async def） ：定义异步任务，返回协程对象。</li>
<li>任务（Task） ：由 asyncio.create_task() 创建的调度单元</li>
</ul>
</li>
<li><p>基础并发模型</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> asyncio</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">fetch_data</span>(<span class="params">url</span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;请求 <span class="subst">&#123;url&#125;</span> 开始&quot;</span>)</span><br><span class="line">    <span class="keyword">await</span> asyncio.sleep(<span class="number">1</span>)  <span class="comment"># 模拟网络延迟（非阻塞）</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;请求 <span class="subst">&#123;url&#125;</span> 完成&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">main</span>():</span><br><span class="line">    <span class="comment"># 使用gather并发执行多个协程</span></span><br><span class="line">    <span class="keyword">await</span> asyncio.gather(</span><br><span class="line">        fetch_data(<span class="string">&quot;https://api1.com&quot;</span>),</span><br><span class="line">        fetch_data(<span class="string">&quot;https://api2.com&quot;</span>)</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">asyncio.run(main())  <span class="comment"># 启动事件循环[[1]][[3]][[10]]</span></span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>-pyTutorial</category>
      </categories>
      <tags>
        <tag>-python -multiThread</tag>
      </tags>
  </entry>
  <entry>
    <title>DPU初步了解</title>
    <url>/dpu-introduction/</url>
    <content><![CDATA[<p>想要了解DPU，不可否认的，需要了解各种*PU，知道他们的功能区别，以及为什么需要拆分出这样的单独模块。下面给出AI芯片领域的XPU的含义简介，帮助了解一些相关知识。</p>
<h2 id="XPU含义简介"><a href="#XPU含义简介" class="headerlink" title="XPU含义简介"></a>XPU含义简介</h2><ul>
<li>含义： XPU指将多种不同类型的处理核心（如CPU、GPU、NPU及其他专用加速器IP）深度集成在同一个芯片（SoC）或封装内的异构处理器。其设计目标是通过紧密集成和高效互联，为复杂应用（如客户端AI、边缘计算）提供一体化的高性能、高能效解决方案。</li>
<li>核心特征<ul>
<li>异构集成：不是单一类型的核心，而是“CPU+GPU+NPU+其他”的组合。</li>
<li>统一封装：这些核心通过先进的封装技术（如Foveros、CoWoS）或传统SoC设计集成在一起，共享高速内存池和互连总线。</li>
<li>任务协同：操作系统和驱动程序能够自动或根据开发者指令，将不同任务分发给最适合的核心执行</li>
</ul>
</li>
</ul>
<h2 id="什么是DPU？"><a href="#什么是DPU？" class="headerlink" title="什么是DPU？"></a>什么是DPU？</h2><p>DPU（Data Processing Unit，数据处理单元）是专门设计用于数据中心基础设施处理的处理器。它主要承担数据移动、安全和网络处理等任务，将CPU从这些工作中解放出来，专注于应用程序执行。</p>
<h3 id="DPU计算架构组成："><a href="#DPU计算架构组成：" class="headerlink" title="DPU计算架构组成："></a>DPU计算架构组成：</h3><ol>
<li><p>控制处理器（Arm Cortex或RISC-V核心）</p>
<ul>
<li>运行控制平面软件</li>
<li>管理系统资源</li>
</ul>
</li>
<li><p>数据平面处理器</p>
<ul>
<li>可编程数据路径（P4语言支持）</li>
<li>硬件加速引擎</li>
</ul>
</li>
<li><p>专用加速器</p>
<ul>
<li>加密&#x2F;解密引擎（AES, SHA）</li>
<li>正则表达式匹配</li>
<li>压缩&#x2F;解压缩引擎</li>
</ul>
</li>
</ol>
<h3 id="主要DPU产品架构对比"><a href="#主要DPU产品架构对比" class="headerlink" title="主要DPU产品架构对比"></a>主要DPU产品架构对比</h3><table>
<thead>
<tr>
<th align="center">厂商</th>
<th align="center">产品系列</th>
<th align="center">CPU核心</th>
<th align="center">网络接口</th>
<th align="center">特殊功能</th>
<th align="center">目标市场</th>
</tr>
</thead>
<tbody><tr>
<td align="center">NVIDIA</td>
<td align="center">BlueField系列</td>
<td align="center">Arm Cortex-A72&#x2F;A78</td>
<td align="center">25&#x2F;100&#x2F;200GbE</td>
<td align="center">融合GPU加速</td>
<td align="center">超大规模云、HPC</td>
</tr>
<tr>
<td align="center">Intel</td>
<td align="center">IPU系列</td>
<td align="center">Xeon核心 + FPGA</td>
<td align="center">100&#x2F;200&#x2F;400GbE</td>
<td align="center">FPGA可编程性</td>
<td align="center">电信、企业云</td>
</tr>
<tr>
<td align="center">AMD</td>
<td align="center">Pensando系列</td>
<td align="center">定制处理器</td>
<td align="center">25&#x2F;100GbE</td>
<td align="center">分布式服务卡</td>
<td align="center">企业数据中心</td>
</tr>
<tr>
<td align="center">Marvell</td>
<td align="center">OCTEON系列</td>
<td align="center">多核MIPS&#x2F;ARM</td>
<td align="center">10-200GbE</td>
<td align="center">安全处理</td>
<td align="center">网络设备、安全</td>
</tr>
<tr>
<td align="center">Broadcom</td>
<td align="center">Stingray系列</td>
<td align="center">Arm Neoverse N1</td>
<td align="center">100&#x2F;200GbE</td>
<td align="center">存储虚拟化</td>
<td align="center">存储服务器</td>
</tr>
</tbody></table>
<p>可以看到大部分DPU产品的应用场景是大规模的数据存储&#x2F;调用。</p>
<h2 id="XPU和DPU的对比"><a href="#XPU和DPU的对比" class="headerlink" title="XPU和DPU的对比"></a>XPU和DPU的对比</h2><table>
<thead>
<tr>
<th align="left">维度</th>
<th align="left">XPU（如GPU&#x2F;TPU）</th>
<th align="left">DPU</th>
</tr>
</thead>
<tbody><tr>
<td align="left">主要目标</td>
<td align="left">计算性能最大化</td>
<td align="left">数据移动效率最大化</td>
</tr>
<tr>
<td align="left">优化指标</td>
<td align="left">FLOPS&#x2F;TOPS（计算吞吐量）</td>
<td align="left">数据包处理速率、延迟</td>
</tr>
<tr>
<td align="left">工作负载</td>
<td align="left">规则的计算密集型任务</td>
<td align="left">不规则的数据移动任务</td>
</tr>
<tr>
<td align="left">内存系统</td>
<td align="left">高带宽、大容量</td>
<td align="left">低延迟、高队列深度</td>
</tr>
<tr>
<td align="left">编程模型</td>
<td align="left">SIMD&#x2F;SIMT并行</td>
<td align="left">流水线处理、事件驱动</td>
</tr>
</tbody></table>
<h3 id="可以简单分析XPU和DPU的关系如下"><a href="#可以简单分析XPU和DPU的关系如下" class="headerlink" title="可以简单分析XPU和DPU的关系如下"></a>可以简单分析XPU和DPU的关系如下</h3><ul>
<li><p>并列关系（在系统层面）：</p>
<ul>
<li>XPU：主要负责应用程序的计算任务（通用计算+图形渲染+AI推理）。</li>
<li>DPU：主要负责系统的数据基础设施任务（网络、存储、安全虚拟化）。<br>  它们在现代服务器或PC中，可以是两个独立的芯片，协同工作。例如，一台AI服务器可能同时装有集成CPU+GPU+NPU的XPU芯片和负责高速网络与存储卸载的DPU卡。</li>
</ul>
</li>
<li><p>包含关系（在未来演进中）：</p>
<p>  随着芯片集成度不断提高，未来的XPU（集成芯片）有可能将DPU的部分核心功能作为IP模块集成进来。例如，未来的客户端XPU可能会集成一个轻量级的网络&#x2F;安全引擎。同样，未来的服务器DPU也可能集成更强的通用计算或AI加速核心。<br>  英特代号“Falcon Shores”的数据中心XPU，其目标就是融合至强CPU与Xe GPU，虽然不叫DPU，但它在处理HPC和AI工作负载时，必然会融合高性能的数据传输能力。</p>
</li>
<li><p>侧重点不同：</p>
<p>  XPU的集成，侧重于提升终端应用的性能和能效，其集成对象是计算类核心。<br>  DPU的设计，侧重于卸载和管理数据中心基础设施，其核心能力是数据移动和策略执行。</p>
</li>
</ul>
<hr>
<p>之所以要花这么大的篇幅去说XPU，就是想表达，DPU其实也是<em>PU的一种，将特定功能从CPU上独立出来，用一个单独、高效的模块去执行这一部分功能。<br>整个</em>PU系统的最终能效比计算公式为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">系统能效比 = Σ(性能_i) / Σ(功耗_i)</span><br></pre></td></tr></table></figure>

<p>而每种硬件都有其最优工作负载区间，分离出来既可以专项优化，又可以通过专用硬件加速可并行部分。                                                          </p>
<hr>
]]></content>
      <tags>
        <tag>--DPU --network</tag>
      </tags>
  </entry>
</search>
